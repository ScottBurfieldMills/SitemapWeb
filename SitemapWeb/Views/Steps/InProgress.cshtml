<script type="text/x-template" id="sitemap-inprogress">
    <div>
        <h1>In Progress - {{url}}</h1>
        
        <div class="alert alert-info">
            <p :class="parsingStepClass">
                1. Loading and parsing sitemap
            </p>
            <p :class="validatingStepClass">
                2. Validating sitemap
            </p>
        </div>

        <table class="table table-striped table-hover">
            <thead>
                <tr>
                    <th>Url</th>
                    <th>Status Code</th>
                </tr>
            </thead>
            <tbody>
                <tr v-for="item in results">
                    <td>{{item.url}}</td>
                    <td>{{item.statusCode}}</td>
                </tr>
            </tbody>
        </table>
    </div>
</script>