<script type="text/x-template" id="sitemap-inprogress">
    <div>
        <h1>In Progress - {{url}}</h1>

        <table class="table table-striped table-hover">
            <thead>
                <tr>
                    <th>Url</th>
                    <th>Status Code</th>
                </tr>
            </thead>
            <tbody>
                <tr v-for="item in results">
                    <td>{{item.url}}</td>
                    <td>{{item.statusCode}}</td>
                </tr>
            </tbody>
        </table>
    </div>
</script>